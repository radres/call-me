{
  "name": "callme-claude-channel",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild channel.mjs --bundle --platform=node --format=esm --minify --outfile=../dist/channel.mjs",
    "check": "node --check channel.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
